# Metis Python runtime

This package contains the command-line training, inference, recording, export,
environment and monitoring code used by the Metis Godot add-on.

The public command is:

```bash
metis --help
```

The source repository keeps the historical `python/train.py`, `python/run.py`,
`python/recorder.py` and `python/export.py` entry points available.

