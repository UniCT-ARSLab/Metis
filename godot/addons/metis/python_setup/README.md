# Python runtime payload

Release builds place the matching `metis_rl` wheel, requirement profiles and
`runtime_manifest.json` in this directory. They are generated from the
repository's canonical `python/` source by `packaging/build_release.py`.

